using UnityEngine;

public class Mirror : MonoBehaviour
{
    // only exists to allow other classes to recognize mirror objects, as opposed to normal ground
    // otherwise, contains absolutely nothing
}
